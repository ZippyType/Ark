import tkinter as tk

win = tk.Tk()
win.title("Event Binding")

label = tk.Label(win, text="Press any key...")
label.pack()

fn on_key(event) (
    label.config(text="Key: " + event.char)
)

win.bind("<Key>", on_key)
label.focus()

win.mainloop()