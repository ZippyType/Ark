import tkinter as tk

win = tk.Tk()
win.title("Simple GUI")

label = tk.Label(win, text="Hello from Ark!")
label.pack()

button = tk.Button(win, text="Click Me", command=lambda: print("Clicked!"))
button.pack()

entry = tk.Entry(win)
entry.pack()

win.mainloop()