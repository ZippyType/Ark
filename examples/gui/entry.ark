win = window("Entry Example", 300, 200)

label(win, "Enter your name:").pack()

input_field = entry(win)
input_field.pack(pady=5)

fn show_text() (
    message(input_field.get(), "Input")
)

button(win, "Show Text", show_text).pack()

win.mainloop()