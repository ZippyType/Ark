import tkinter as tk

win = tk.Tk()
win.title("Entry Example")

tk.Label(win, text="Enter your name:").pack()

entry = tk.Entry(win)
entry.pack(pady=5)

fn show_text() (
    print(entry.get())
)

tk.Button(win, text="Show Text", command=show_text).pack()

win.mainloop()