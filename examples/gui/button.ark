win = window("Button Example", 300, 200)

count = 0

fn on_click() (
    global count
    count = count + 1
    lbl.config(text="Clicks: " + str(count))
)

lbl = label(win, "Clicks: 0", font=Font("Arial", 20))
lbl.pack(pady=20)

button(win, "Click Me", on_click).pack()

win.mainloop()